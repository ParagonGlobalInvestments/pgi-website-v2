# =====================================================
# PRODUCTION ENVIRONMENT VARIABLES
# =====================================================
# Copy this file to .env.local for production deployment

# =====================================================
# SUPABASE CONFIGURATION (REQUIRED)
# =====================================================
NEXT_PUBLIC_SUPABASE_URL=your_production_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_production_anon_key
# Service role key for server-side admin operations (NEVER expose to browser!)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# =====================================================
# FEATURE FLAGS (PRODUCTION DEFAULTS)
# =====================================================
# These control what features are visible in production
# All features are DISABLED by default for safety

# Dashboard Stats (internships count, members count, chapters count)
# Set to 'true' when ready to show stats to members
NEXT_PUBLIC_SHOW_STATS=false

# Internships Feature (posting, viewing, browsing)
# Set to 'true' when internships feature is fully tested and ready
NEXT_PUBLIC_ENABLE_INTERNSHIPS=false

# Admin Features (always enabled unless you need to disable for testing)
NEXT_PUBLIC_ENABLE_ADMIN_FEATURES=true

# =====================================================
# ANALYTICS (OPTIONAL)
# =====================================================
# NEXT_PUBLIC_POSTHOG_KEY=your_production_posthog_key
# NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# =====================================================
# DEPLOYMENT INFO
# =====================================================
NODE_ENV=production

